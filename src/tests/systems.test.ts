import{describe,expect,it}from'vitest';import{validate}from'../state/persistence';import{finalPortalUnlocked}from'../systems/progressionSystem';import{validPlaylistUrl,wheelRotationForIndex}from'../systems/wheelSystem';
describe('safe persistence',()=>{it('falls back for corrupt data',()=>expect(validate(null).version).toBe(2));it('filters malformed stars',()=>expect(validate({stars:[0,8,'x']}).stars).toEqual([0]))});describe('progression',()=>{it('keeps portal locked before core experiences',()=>expect(finalPortalUnlocked({firstLetterRead:true,wheelSpun:false,completedGames:0,giftCompleted:false,moodSelected:false,starsDiscovered:0},{requiredCoreActivities:3,featureFlags:{}})).toBe(false));it('unlocks after configured activities',()=>expect(finalPortalUnlocked({firstLetterRead:true,wheelSpun:true,completedGames:2,giftCompleted:true,moodSelected:false,starsDiscovered:0},{requiredCoreActivities:3,featureFlags:{}})).toBe(true))});describe('wheel and playlist',()=>{it('calculates a deterministic rotation',()=>expect(wheelRotationForIndex(0)).toBe(2142));it('accepts safe Spotify links only',()=>{expect(validPlaylistUrl('https://open.spotify.com/playlist/a')).toBe(true);expect(validPlaylistUrl('javascript:alert(1)')).toBe(false)})});
